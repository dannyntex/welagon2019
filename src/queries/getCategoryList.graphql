# TODO: get only active categories from graphql when it is ready
query categoryList($id: Int!) {
    category(id: $id) {
        id
        children {
            id
            name
            url_key
            url_path
            image
            include_in_menu

        }
    }
}
